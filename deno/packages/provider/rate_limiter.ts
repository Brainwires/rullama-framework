/**
 * Token-bucket rate limiter. The implementation lives in `@rullama/core`
 * (`RateLimiter`, `RateLimitedClient`) and is re-exported here unchanged.
 *
 * @module
 */
export {
  RateLimitedClient,
  type RateLimitedClientOptions,
  RateLimiter,
} from "@rullama/core";
