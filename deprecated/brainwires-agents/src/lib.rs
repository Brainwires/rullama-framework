#![deprecated(
    since = "0.10.1",
    note = "renamed to `brainwires-agent` (singular). The framework's pluralization rule (singular for capability domains) was applied workspace-wide; switch your dep and your imports. There is no re-export shim."
)]
//! `brainwires-agents` is **deprecated** as of 0.10.1.
//!
//! Renamed to [`brainwires-agent`](https://crates.io/crates/brainwires-agent) —
//! the framework now uses singular crate names for capability domains
//! (`brainwires-tool`, `brainwires-provider`, `brainwires-permission`,
//! `brainwires-agent`, etc.).
//!
//! There is no re-export shim.
