[package]
name = "brainwires-audio"
version = "0.6.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.91"
description = "DEPRECATED: use brainwires-hardware with feature \"audio\" instead"
repository = "https://github.com/Brainwires/rullama-framework"
readme = "README.md"
keywords = ["deprecated"]

[dependencies]
brainwires-hardware = { version = "0.6.0", features = ["audio"] }

[workspace]
