#![deprecated(
    since = "0.10.1",
    note = "moved to `rullama-finetune` in the sibling `rullama` workspace (https://github.com/Brainwires/rullama). There is no re-export shim — switch your dependency to `rullama-finetune`."
)]
//! `brainwires-finetune-local` is **deprecated** as of 0.10.1.
//!
//! The Burn-backed local LoRA / QLoRA / DoRA fine-tuning pipeline moved out
//! to the sibling `rullama` workspace as `rullama-finetune` in v0.11.0 of
//! the Brainwires framework. The framework no longer carries a low-level
//! training stack — embed `rullama-finetune` directly.
//!
//! There is no re-export shim.
