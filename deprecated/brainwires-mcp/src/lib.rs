#![deprecated(
    since = "0.10.1",
    note = "renamed to `brainwires-mcp-client` for symmetry with `brainwires-mcp-server`. The old name was ambiguous (server vs client). There is no re-export shim — switch your dep and your imports."
)]
//! `brainwires-mcp` is **deprecated** as of 0.10.1.
//!
//! Renamed to [`brainwires-mcp-client`](https://crates.io/crates/brainwires-mcp-client)
//! to disambiguate from [`brainwires-mcp-server`](https://crates.io/crates/brainwires-mcp-server).
//! There is no re-export shim.
