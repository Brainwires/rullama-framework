[package]
name = "brainwires-mesh"
version = "0.2.1"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.91"
description = "DEPRECATED: use brainwires-agent-network with feature \"mesh\" instead"
repository = "https://github.com/Brainwires/rullama-framework"
readme = "README.md"
keywords = ["deprecated"]

[dependencies]
brainwires-agent-network = { version = "0.3", features = ["mesh"] }

[workspace]
