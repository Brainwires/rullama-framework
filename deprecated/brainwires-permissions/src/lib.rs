#![deprecated(
    since = "0.10.1",
    note = "renamed to `brainwires-permission` (singular) for consistency with the rest of the framework. There is no re-export shim — switch your dep and your imports."
)]
//! `brainwires-permissions` is **deprecated** as of 0.10.1.
//!
//! Renamed to [`brainwires-permission`](https://crates.io/crates/brainwires-permission)
//! (singular) for consistency with the framework's "singular for capability
//! domains" naming rule. There is no re-export shim.
