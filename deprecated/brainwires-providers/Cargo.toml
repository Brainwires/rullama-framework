[package]
name = "brainwires-providers"
version = "0.10.1"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.91"
description = "DEPRECATED: renamed to `brainwires-provider` (singular) and split — speech (TTS / STT) extracted to `brainwires-provider-speech`. See README."
repository = "https://github.com/Brainwires/brainwires-framework"
readme = "README.md"
keywords = ["deprecated"]

[lib]
path = "src/lib.rs"

[workspace]
