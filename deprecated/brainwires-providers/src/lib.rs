#![deprecated(
    since = "0.10.1",
    note = "renamed to `brainwires-provider` (singular) for consistency with the framework's naming rule. Speech (TTS / STT) clients also extracted to `brainwires-provider-speech`. There is no re-export shim — switch your dep and your imports per the README."
)]
//! `brainwires-providers` is **deprecated** as of 0.10.1.
//!
//! Two changes happened simultaneously:
//!
//! - Renamed to [`brainwires-provider`](https://crates.io/crates/brainwires-provider)
//!   (singular, matches the rest of the framework).
//! - Speech (TTS / STT) clients extracted to
//!   [`brainwires-provider-speech`](https://crates.io/crates/brainwires-provider-speech).
//!
//! There is no re-export shim.
