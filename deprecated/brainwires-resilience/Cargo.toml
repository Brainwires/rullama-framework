[package]
name = "brainwires-resilience"
version = "0.10.1"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.91"
description = "DEPRECATED: renamed to `brainwires-call-policy` to actually describe what it does (policies on outbound calls — retry / circuit / budget / cache). See README."
repository = "https://github.com/Brainwires/brainwires-framework"
readme = "README.md"
keywords = ["deprecated"]

[lib]
path = "src/lib.rs"

[workspace]
