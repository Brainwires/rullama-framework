#![deprecated(
    since = "0.10.1",
    note = "moved to `rullama-training` in the sibling `rullama` workspace (https://github.com/Brainwires/rullama). There is no re-export shim — switch your dependency to `rullama-training`."
)]
//! `brainwires-training` is **deprecated** as of 0.10.1.
//!
//! The from-scratch training placeholder moved out to the sibling `rullama`
//! workspace as `rullama-training` in v0.11.0 of the Brainwires framework.
//! The framework no longer carries a low-level training stack — embed
//! `rullama-training` directly.
//!
//! There is no re-export shim.
