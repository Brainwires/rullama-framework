# Engine sub-workspace toolchain. Nested under the brainwires repo but pins the
# wasm target + components the engine build needs (the root pin omits them).
[toolchain]
channel = "1.91"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
